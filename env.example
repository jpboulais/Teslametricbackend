# Tesla API Configuration
TESLA_CLIENT_ID=2f74f070-ee5c-4d14-a5d3-2cb48dede96c
TESLA_CLIENT_SECRET=ta-secret.!1^HpGhJerArNHg@
# Must match exactly a Redirect URI in Tesla Developer Portal. Tesla does not allow localhost.
# Use your production callback URL (e.g. Railway). Callback path: API_BASE_PATH + /auth/tesla/callback.
TESLA_REDIRECT_URI=https://tesla-backend-production.up.railway.app/api/v1/auth/tesla/callback

# Domain used for Tesla virtual key link (https://www.tesla.com/_ak/<domain>). If unset, derived from TESLA_REDIRECT_URI host.
# Should match your Tesla Developer Portal "Allowed Origin" (e.g. tesla-backend-production.up.railway.app).
# TESLA_DEVELOPER_DOMAIN=tesla-backend-production.up.railway.app
TESLA_AUTH_BASE_URL=https://auth.tesla.com
TESLA_API_BASE_URL=https://fleet-api.prd.na.vn.cloud.teslamotors.com

# Backend Server Configuration
PORT=3000
NODE_ENV=development
API_BASE_PATH=/api/v1

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tesla_driving_metrics
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tesla_driving_metrics
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=tesla_driving_metrics_jwt_secret_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Session Configuration
SESSION_SECRET=tesla_driving_metrics_session_secret_change_this_in_production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,tesladriving://

# API Configuration
POLLING_INTERVAL_MS=10000
MAX_REQUESTS_PER_DAY=1000

# Logging
LOG_LEVEL=info

# Tesla public key PEM (optional): full PEM text for Fleet API / virtual key. If set, served at /.well-known/appspecific/com.tesla.3p.public-key.pem when keys/public-key.pem is missing (e.g. on Railway). Generate with: node src/utils/generateKeys.js
# TESLA_PUBLIC_KEY_PEM=

# Telemetry ingest (optional): if set, POST /telemetry/ingest requires header X-Telemetry-Secret: <value>
# TELEMETRY_INGEST_SECRET=
